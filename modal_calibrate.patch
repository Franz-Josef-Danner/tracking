diff --git a/Operator/auto_calibrate_operator.py b/Operator/auto_calibrate_operator.py
index 9a34c1d..fa3a4be 100644
--- a/Operator/auto_calibrate_operator.py
+++ b/Operator/auto_calibrate_operator.py
@@
 def register():
     bpy.utils.register_class(KAISERLICHTRACKER_OT_auto_calibrate)

 def unregister():
     bpy.utils.unregister_class(KAISERLICHTRACKER_OT_auto_calibrate)
+
+
+# =============================================================================
+#  Modal Wrapper für Auto-Calibrate (asynchron mit Tracking-Flags)
+# =============================================================================
+
+
+class KAISERLICHTRACKER_OT_auto_calibrate_modal(bpy.types.Operator):
+    """Automatische Kalibrierung (modal, reagiert auf Tracking-Flags)"""
+    bl_idname = "kaiserlich_tracker.auto_calibrate_modal"
+    bl_label = "Auto Calibrate (Modal)"
+    bl_options = {"REGISTER", "INTERNAL"}
+
+    _timer = None
+    _phase = 0  # 0=init, 1=waiting tracking, 2=analysis, 3=done
+
+    def execute(self, context):
+        scene = context.scene
+        scene["kaiserlich_tracking_forward_done"] = False
+        scene["kaiserlich_tracking_backward_done"] = False
+        scene["kaiserlich_tracking_in_progress"] = True
+
+        print("[Kaiserlich Tracker][AutoCalibrate] Initialisiert und startet Tracking ...")
+        try:
+            bpy.ops.kaiserlich_tracker.track_cycle('INVOKE_DEFAULT')
+            bpy.ops.kaiserlich_tracker.track_cycle_backwards('INVOKE_DEFAULT')
+        except Exception as e:
+            self.report({'ERROR'}, f"Tracking konnte nicht gestartet werden: {e}")
+            return {'CANCELLED'}
+
+        wm = context.window_manager
+        self._timer = wm.event_timer_add(0.2, window=context.window)
+        wm.modal_handler_add(self)
+        self._phase = 1
+        return {'RUNNING_MODAL'}
+
+    def modal(self, context, event):
+        if event.type == 'ESC':
+            print("[Kaiserlich Tracker][AutoCalibrate] Abgebrochen durch Benutzer.")
+            self._finish(context, cancelled=True)
+            return {'CANCELLED'}
+
+        if event.type != 'TIMER':
+            return {'PASS_THROUGH'}
+
+        scene = context.scene
+
+        # -----------------------------
+        # 🕒 Phase 1: Tracking abwarten
+        # -----------------------------
+        if self._phase == 1:
+            f_done = scene.get("kaiserlich_tracking_forward_done", False)
+            b_done = scene.get("kaiserlich_tracking_backward_done", False)
+            if f_done and b_done:
+                print("[Kaiserlich Tracker][AutoCalibrate] Tracking abgeschlossen → starte Analyse ...")
+                scene["kaiserlich_tracking_in_progress"] = False
+                self._phase = 2
+
+        # -----------------------------
+        # 📊 Phase 2: Analyse starten
+        # -----------------------------
+        if self._phase == 2:
+            try:
+                bpy.ops.kaiserlich_tracker.auto_calibrate('EXEC_DEFAULT')
+                self._phase = 3
+            except Exception as e:
+                print(f"[Kaiserlich Tracker][AutoCalibrate] Fehler bei Analyse: {e}")
+                self._finish(context, cancelled=True)
+                return {'CANCELLED'}
+
+        # -----------------------------
+        # ✅ Phase 3: Abschluss
+        # -----------------------------
+        if self._phase == 3:
+            print("[Kaiserlich Tracker][AutoCalibrate] ✓ abgeschlossen.")
+            self._finish(context)
+            return {'FINISHED'}
+
+        return {'RUNNING_MODAL'}
+
+    def _finish(self, context, cancelled=False):
+        wm = context.window_manager
+        if self._timer:
+            wm.event_timer_remove(self._timer)
+        if not cancelled:
+            print("[Kaiserlich Tracker][AutoCalibrate] Modal beendet.")
+        else:
+            print("[Kaiserlich Tracker][AutoCalibrate] Modal abgebrochen.")
+
+
+def register():
+    bpy.utils.register_class(KAISERLICHTRACKER_OT_auto_calibrate_modal)
+
+
+def unregister():
+    bpy.utils.unregister_class(KAISERLICHTRACKER_OT_auto_calibrate_modal)
diff --git a/Operator/track_operator.py b/Operator/track_operator.py
index 6cf7c63..08a97de 100644
--- a/Operator/track_operator.py
+++ b/Operator/track_operator.py
@@
     def _finish(self, context, cancelled: bool = False):
         wm = context.window_manager
         if self._timer:
             wm.event_timer_remove(self._timer)
         self._timer = None

         # Selektion wiederherstellen
         clip = getattr(context.space_data, "clip", None)
         if clip and hasattr(clip, "tracking"):
             for tr in clip.tracking.tracks:
                 tr.select = (tr.name in self._original_selected)

         try:
             reset_to_frame(context, self._start_frame)
         except Exception:
             pass

-        print(
-            "[Kaiserlich Tracker][Modal] Zyklus beendet."
-            if not cancelled else
-            "[Kaiserlich Tracker][Modal] Abgebrochen."
-        )
+        try:
+            context.scene["kaiserlich_tracking_forward_done"] = True
+        except Exception:
+            pass
+
+        print(
+            "[Kaiserlich Tracker][Modal] Zyklus beendet."
+            if not cancelled else
+            "[Kaiserlich Tracker][Modal] Abgebrochen."
+        )
diff --git a/Operator/track_operator_backwards.py b/Operator/track_operator_backwards.py
index 8cf19ad..c72b841 100644
--- a/Operator/track_operator_backwards.py
+++ b/Operator/track_operator_backwards.py
@@
     def _finish(self, context, cancelled: bool = False):
         wm = context.window_manager
         if self._timer:
             wm.event_timer_remove(self._timer)
         self._timer = None

         clip = getattr(context.space_data, "clip", None)
         if clip and hasattr(clip, "tracking"):
             for tr in clip.tracking.tracks:
                 tr.select = (tr.name in self._original_selected)

         try:
             reset_to_frame(context, self._start_frame_saved)
         except Exception:
             pass

-        print(
-            "[Kaiserlich Tracker][Modal Rückwärts] Zyklus beendet."
-            if not cancelled else
-            "[Kaiserlich Tracker][Modal Rückwärts] Abgebrochen."
-        )
+        try:
+            context.scene["kaiserlich_tracking_backward_done"] = True
+        except Exception:
+            pass
+
+        print(
+            "[Kaiserlich Tracker][Modal Rückwärts] Zyklus beendet."
+            if not cancelled else
+            "[Kaiserlich Tracker][Modal Rückwärts] Abgebrochen."
+        )